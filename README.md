# School-meal-system
**МОСКОВСКАЯ ПРЕДПРОФЕССИОНАЛЬНАЯ ОЛИМПИАДА ШКОЛЬНИКОВ**
Профиль «Информационные технологии»
Командный кейс №2 «Управление столовой»

---

## Содержание
- [Команда](#команда)
- [Backend-часть](#backend-часть)
- [Frontend-часть](#frontend-часть)
- [Запуск всего проекта](#запуск-всего-проекта)

---

## Команда
- Сунгуров Владислав, 10 класс, **Fullstack / Team Lead**
- Косяк Данила, 10 класс, **Frontend**
- Медведицков Тимофей, 10 класс, **Frontend**
- Крымский Кирилл, 10 класс, **Frontend**
- Волынец Павел, 10 класс, **Frontend**

---

## Backend-часть

### Стек технологий

| Компонент              | Технология                    | Версия / Примечание                                 |
|------------------------|-------------------------------|-----------------------------------------------------|
| Язык программирования  | Python                        | 3.14                                                |
| Фреймворк              | FastAPI                       | последняя стабильная                                |
| ORM                    | SQLAlchemy 2.0 (Core + ORM)   | современный стиль, полный типизированный код        |
| Аутентификация         | JWT + Argon2id (passlib)      | современный стандарт, стойкий к brute-force         |
| База данных            | PostgreSQL                    | 17                                                  |
| Администрирование БД   | pgAdmin 4                     | веб-интерфейс                                       |
| Контейнеризация        | Docker + Docker Compose       | многосервисный стек (postgres + backend + pgadmin)  |
| Менеджер зависимостей  | uv                            | сверхбыстрый аналог pip + lock-файл                 |

### Почему выбран именно такой стек

| Технология | Почему выбрали |
| ---------- | -------------- |
| **FastAPI**      | - Самый быстрый Python-фреймворк (на уровне Node.js/Go)<br>- Автоматическая генерация OpenAPI/Swagger (готовый интерактивный docs)<br>- Полная поддержка async/await<br>- Встроенная валидация через Pydantic v2<br>- Идеален для олимпиадных кейсов — минимум бойлерплейта, максимум функциональности |
| **SQLAlchemy 2.0**| - Самая мощная и гибкая ORM для Python<br>- Полностью типизированный код (MyPy/Pyright без проблем)<br>- Поддержка миграций через Alembic (в дальнейшем)<br>- Отличная производительность даже при сложных запросах |
| **PostgreSQL**   | - Самая продвинутая open-source СУБД<br>- Поддержка JSONB, полнотекстовый поиск, триггеры, оконные функции<br>- Надёжность на уровне enterprise-решений<br>- Есть опыт у всей команды |
| **JWT + Argon2id**| - Argon2 - победитель конкурса Password Hashing Competition (2015), стойкий к GPU-атакам<br>- JWT - стандарт де-факто для SPA + мобильных клиентов<br>- Токен хранит email, full_name, parallel и role → минимум нагрузки на БД |
| **uv**           | - В 10–100 раз быстрее pip + venv<br>- Создаёт полностью воспроизводимый lock-файл (uv.lock) |
| **Docker Compose**| - Один docker-compose up - полностью рабочий стек (БД + API + pgAdmin)<br>- Полная изоляция окружений<br> |

### Структура проекта (backend/app)

```
backend/
|-- 📂 app/
|   |-- 📄 main.py          - запуск FastAPI, все роуты
|   |-- 📄 models.py        - SQLAlchemy модели (User, Dish, Order, Review, PurchaseRequest)
|   |-- 📄 schemas.py       - Pydantic модели (валидация входящих/исходящих данных)
|   |-- 📄 crud.py          - все операции с БД (чистый код, легко тестировать)
|   |-- 📄 database.py      - engine, SessionLocal, Base
|   |-- 📄 auth.py          - хеширование паролей (Argon2), создание/проверка JWT
|   |-- 📄 dependencies.py  - get_current_user, require_student/chef/admin
|   |-- 📄 seed.py          - заполнение БД тестовыми данными (30 заказов, отзывы, заявки и т.д.)
|-- ⚙️ .dockerignore        - игнорируемые файлы при сборке изображения
|-- ⚙️ .gitignore           - игнорируемые файлы при трекинге изменений через git
|-- ⚙️ .pythonversion       - версия python (3.14)
|-- ⚙️ Dockerfile           - файл для сбокри изображения
|-- ⚙️ pytproject.toml      - фиксация зависимостей
|-- ⚙️ uv.lock              - фиксация зависимостей
```

### Реализованные роли и права доступа

| Роль       | Основные возможности                                                                                  | Защищено через dependency          |
|------------|--------------------------------------------------------------------------------------------------------|------------------------------------|
| **student**| Регистрация/логин, просмотр меню, создание заказа, пополнение баланса, отметка получения, отзывы     | `require_student` |
| **chef**   | Просмотр всех заказов, заказов на сегодня, создание заявок на закупку, просмотр своих заявок          | `require_chef` |
| **admin**  | Всё, что могут студент и повар + управление меню (CRUD блюд), просмотр всех заявок, смена статуса, статистика и отчёты | `require_admin`                    |

Админ также может выполнять действия от имени других ролей (в рамках бекенда).

### Основные эндпоинты API (`http://localhost:8000/docs` - будет доступен после поднятия бекенда)

#### Публичные
```
POST   /register          — регистрация (по умолчанию student)
POST   /login             — получение JWT-токена
GET    /                  — приветствие
GET    /health            — health-check
POST   /seed              — (dev) заполнить тестовыми данными
```

#### Ученик
```
GET    /me                  → профиль + баланс
PATCH  /me/profile          → обновить аллергии/предпочтения
POST   /me/balance          → пополнить баланс
GET    /menu                → меню (?is_breakfast=true/false)
POST   /orders              → создать заказ
GET    /orders/my           → мои заказы
POST   /orders/{id}/receive → отметить как полученный
POST   /reviews             → оставить отзыв
GET    /dishes/{id}/reviews → отзывы о блюде
```

#### Повар
```
GET    /chef/orders               → все заказы
GET    /chef/orders/today         → заказы на сегодня
GET    /chef/dishes               → блюда с остатками
POST   /chef/purchase-requests    → создать заявку на закупку
GET    /chef/purchase-requests/my → мои заявки
```

#### Администратор
```
GET    /admin/statistics/payments      → статистика оплат
GET    /admin/statistics/attendance    → статистика посещаемости
GET    /admin/purchase-requests        → все заявки
PATCH  /admin/purchase-requests/{id}   → изменить статус (approved/rejected)
POST   /admin/dishes                   → создать блюдо
PATCH  /admin/dishes/{id}              → обновить блюдо
DELETE /admin/dishes/{id}              → удалить блюдо
GET    /admin/reports/payments         → полный отчёт по оплатам (JSON)
```

### Безопасность

- Все защищённые эндпоинты требуют `Authorization: Bearer <token>`
- Пароли хешируются через **Argon2id** (рекомендованные OWASP параметры)
- Защита от повторной регистрации по email и ФИО
- Защита от заказа при недостатке средств или нулевом остатке блюда
- CORS настроен на `allow_origins=["*"]` (для демо; в продакшене будет ограничен)

### Тестовые данные (seed)

После первого запуска `/seed` создаётся полностью рабочая система:

- 5 пользователей (3 ученика, 1 повар, 1 админ)
- 12 блюд (5 завтраков + 7 обедов)
- 30 заказов за последние 7 дней
- 7 отзывов
- 5 заявок на закупку с разными статусами

Логины для быстрого входа:
```
admin       → admin / adminpass123
chef_ivan   → chef_ivan / chefpass123
ivanov      → ivanov / password123
```

### Деплой через Docker (одной командой)

```bash
docker compose up
```

Сервисы:
- `backend`  → http://localhost:8000
- `postgres` → http://localhost:5432
- `pgadmin`  → http://localhost:5050

Данные для входа и подключения к БД есть в .env в корне проекта

---

## Frontend-часть

### Стек технологий

| Компонент              | Технология                    | Версия / Примечание                                 |
|------------------------|-------------------------------|-----------------------------------------------------|
| Язык программирования  | JavaScript (с JSX)            | Современный стандарт                                |
| Фреймворк              | React                         | 19.2.0 (с использованием Vite)                     |
| Роутинг                | React Router                  | 7.13.0 (модульная архитектура приложения)           |
| Стилизация             | Tailwind CSS + DaisyUI        | Адаптивный дизайн, готовые компоненты               |
| HTTP-клиент            | Axios                         | Для взаимодействия с backend API                    |
| Управление состоянием  | React Context API             | Для аутентификации и управления сессией             |
| Иконки                 | Lucide React                  | Легковесные иконки                                  |
| Уведомления            | React Hot Toast               | Для отображения сообщений пользователю              |
| Форматирование         | Prettier                      | Единый стиль кода                                   |
| Линтинг                | ESLint                        | Контроль качества кода                              |

### Почему выбран именно такой стек

| Технология | Почему выбрали |
| ---------- | -------------- |
| **React**      | - Компонентный подход, высокая производительность<br>- Большое комьюнити и экосистема<br>- Отличная интеграция с REST API<br>- Подходит для SPA-приложений |
| **Vite**       | - Молниеносно быстрая загрузка и HMR (Hot Module Replacement)<br>- Современный сборщик, замена Webpack/Create React App<br>- Минимальная конфигурация |
| **Tailwind CSS** | - Ускоряет разработку интерфейсов<br>- Utility-first подход<br>- Отличная адаптивность |
| **DaisyUI**    | - Готовые компоненты поверх Tailwind<br>- Темизация по умолчанию<br>- Ускоряет создание UI |
| **React Router** | - Мощный инструмент для маршрутизации<br>- Поддержка защищенных маршрутов<br>- Интеграция с аутентификацией |
| **Axios**      | - Обработка HTTP-запросов с перехватчиками<br>- Перехват ошибок<br>- Поддержка JWT-токенов |

### Структура проекта (school-meal-system/src)

```
school-meal-system/
|-- 📂 public/              - статические файлы
|-- 📂 src/                  - исходный код приложения
|   |-- 📂 api/              - сервисы для работы с API
|   |-- 📂 assets/           - изображения, шрифты и т.д.
|   |-- 📂 components/       - переиспользуемые компоненты
|   |   |-- 📂 layout/       - основные макеты приложения
|   |-- 📂 context/          - контексты React (аутентификация)
|   |-- 📂 pages/            - страницы приложения
|   |   |-- 📂 auth/         - страницы авторизации
|   |   |-- 📂 student/      - страницы для студентов
|   |   |-- 📂 chef/         - страницы для поваров
|   |   |-- 📂 admin/        - страницы для администраторов
|   |-- 📄 App.jsx          - основной компонент маршрутизации
|   |-- 📄 index.css        - глобальные стили
|   |-- 📄 main.jsx         - точка входа в приложение
|-- ⚙️ .gitignore           - игнорируемые файлы git
|-- ⚙️ .prettierignore      - игнорируемые файлы Prettier
|-- ⚙️ .prettierrc          - конфигурация форматирования
|-- ⚙️ eslint.config.js     - конфигурация линтинга
|-- ⚙️ index.html           - основной HTML-файл
|-- ⚙️ package.json         - зависимости и скрипты
|-- ⚙️ vite.config.js       - конфигурация Vite
```

### Архитектура приложения

Frontend реализован как SPA (Single Page Application) с разделением прав доступа по ролям:

| Роль       | Доступные страницы и функции                                                                 |
|------------|----------------------------------------------------------------------------------------------|
| **student**| Авторизация, регистрация, личный кабинет, меню, заказы, профиль, отзывы                     |
| **chef**   | Авторизация, кабинет повара, заказы, остатки, заявки на закупку                             |
| **admin**  | Авторизация, кабинет администратора, управление блюдами, заявками, отчеты                  |

### Защищенные маршруты

Приложение реализует систему защищенных маршрутов на основе ролей:

- `PublicRoute` - для незарегистрированных пользователей (login/register)
- `ProtectedRoute` - для аутентифицированных пользователей с проверкой роли
- Централизованное управление сессией через `AuthContext`

### Основные страницы

#### Публичные
- `/login` - страница входа
- `/register` - страница регистрации

#### Ученик
- `/student` - главная страница ученика
- `/student/menu` - меню с возможностью заказа
- `/student/orders` - история заказов
- `/student/profile` - профиль с балансом и настройками

#### Повар
- `/chef` - главная страница повара
- `/chef/stock` - остатки блюд
- `/chef/requests` - заявки на закупку

#### Администратор
- `/admin` - главная страница администратора
- `/admin/dishes` - управление блюдами
- `/admin/requests` - управление заявками
- `/admin/reports` - отчеты

---

## Запуск всего проекта

Для запуска всего проекта (и бэкенда, и фронтенда) следуйте этим шагам:

### 1. Запуск backend-части (через Docker)

Если у вас Linux, предворительно создайте папку для pgAdmin с нужными правами. **Если у вас винда, то это не требуется**

```bash
# В корне проекта запустить
mkdir -p ./pgadmin-data
sudo chown -R 5050:5050 ./pgadmin-data/
```

Теперь запуск всего backend

```bash
# В корне проекта запустить
docker compose up
```

После этого будут доступны:
- Backend API: `http://localhost:8000`
- PostgreSQL: `http://localhost:5432`
- pgAdmin: `http://localhost:5050`

### 2. Запуск frontend-части (через npm)

В новом терминале:

```bash
# Перейти в директорию frontend
cd school-meal-system

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev
```

После этого frontend будет доступен по адресу: `http://localhost:5173`

### 3. Первичная настройка

После первого запуска backend необходимо заполнить базу данных тестовыми данными:

1. Перейдите на `http://localhost:8000/docs`
2. Найдите эндпоинт `/seed` и выполните его (POST-запрос)
3. Это создаст тестовых пользователей, блюда, заказы и другие данные

Альтернативно, можно выполнить POST-запрос к `http://localhost:8000/seed` с помощью curl или Postman.

### 4. Тестовые учетные записи

После заполнения тестовыми данными можно использовать следующие учетные записи:

- **Администратор**: `admin@school.ru` / `adminpass123`
- **Повар**: `chef@school.ru` / `chefpass123`
- **Ученик**: `student1@school.ru` / `password123`

### 5. Завершение работы

Для остановки всех сервисов:
- Нажмите `Ctrl+C` в каждом из терминалов, где запущены сервисы
- Или выполните `docker compose down` в корне проекта для остановки Docker-контейнеров

---